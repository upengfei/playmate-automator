# 下载页版本未更新

## 已确认

- 截图展示的是平台的 Agent 发布版本，而非设备运行版本。
- GitHub 最新公开 Release 为 agent-v1.8.3，发布日期 2026-09-10；Windows x64、Linux x64、macOS ARM64 三个安装包及 agent-release.json 均已上传。
- 本地默认数据库的 agent_releases 总记录数和当前记录数均为 0。项目 .env 和当前 shell 没有设置 CASE_DB_FILE，未核查独立运行进程的环境。
- src/lib/agent-fleet.server.ts 的 readRelease 在无当前记录或读取异常时回退到写死的 FALLBACK_RELEASE 1.8.2 / 2026-09-05；与截图一致。
- 修改 package.json 的版本只改变源码/构建版本，不会直接登记平台发布记录。
- CI 登记需要 PLATFORM_URL 和 AGENT_RELEASE_TOKEN。缺少配置会跳过；目标为云端平台也不会写入本机 SQLite。尚未验证仓库 Secrets 或对应 CI 登记步骤日志，不能断言是缺密钥。
- 当前代码没有主动从 GitHub 读取最新发布清单的逻辑。
- 本次查询本地 8080 接口连接失败；直接下载 GitHub 清单超时。GitHub REST Release 元数据请求成功，确认了发布与产物存在，但本轮没有下载校验安装包。

## 建议（待确认）

平台按短缓存周期读取 GitHub 最新正式发布及清单，验证版本、产物平台、架构、下载地址和校验值，统一更新下载页与版本查询接口。网络失败保留最后一次有效清单并展示同步状态，首次无有效记录应显示尚未同步而非伪造最新版。提供手动刷新入口。

自动同步的是发布版本、文件名、大小、SHA256 和下载链接；不将源码版本直接视为可下载版本，不改变设备的实际运行版本，也不为每次页面访问增加版本号。

本轮完成诊断和术语澄清，尚未实现自动同步或修改业务数据库。
