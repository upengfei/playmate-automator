# 自动化测试平台 - 任务清单

## 平台端
- [x] 概览看板（关键指标、通过率趋势、失败原因分布）
- [x] 用例管理（列表筛选、关键字积木式编排、脚本实时生成）
- [x] 任务编排与下发（选用例、选执行节点、环境/浏览器/并发/重试）
- [x] 任务进度看板：队列分栏、执行阶段、用例级进度、失败聚合与重试入口
- [x] 实时进度与日志流（步骤级进度 + 日志自动滚动）
- [x] 测试报告分析（历史通过率、用例明细、失败产物入口）
- [x] 执行节点管理：设备信息、版本校验拦截、在线/忙碌/离线心跳监控
- [x] 系统配置（执行策略、节点校验、通知、主题）

## 桌面端 Agent（Electron 风格窗口）
- [x] 窗口外框、菜单栏 + 系统托盘菜单（录制/执行/调试/上传/任务）
- [x] 用例录制、积木式编写、本地执行、断点调试
- [x] 上传到平台（写入平台用例库，来源标记 Agent 录制）
- [x] 接收平台下发任务并回传进度/日志

## 全局
- [x] 浅色 Material 主题 + 一键深色模式（记忆选择）
- [x] 中文界面
- [x] 全部页面改为真实数据库数据（不再使用演示数据）

## 待后续
- [x] 接入云端数据库做真实持久化（用例/任务/报告/节点/升级记录）
- [ ] 真实打包 Electron 客户端与本地 Playwright 执行内核

## 本轮新增（已完成）
- [x] 真正打包的 Electron 客户端（electron/main.cjs + preload.cjs，托盘、单实例、启动/定时检查更新、下载 + SHA256 校验、安装、结果回传）
- [x] 打包脚本：`npm run agent:pack:linux|win|mac`（@electron/packager），已产出 Linux x64 安装包
- [x] 平台端「客户端下载更新」页（安装步骤、环境变量、SHA256、静默部署）
- [x] 版本拦截后自动推送升级包，升级成功自动续跑任务，失败标记「自动升级失败」
- [x] 升级结果回传：平台节点页「升级包推送与结果回传」面板、客户端「版本与更新」面板
- [x] 系统配置新增自动推送升级开关与更新通道
- [x] 公共接口：/api/public/agent/version、/upgrade、/upgrade-report

## 真实客户端与真实执行（已完成）
- [x] 真实 Windows x64 / macOS arm64 客户端压缩包（含 Playwright 运行时）已生成并校验 SHA256
- [x] 客户端启动自动注册节点、30 秒心跳，令牌保存在本机配置
- [x] 平台接口：注册/心跳、用例拉取、用例上传、任务领取、执行结果与日志回传
- [x] 真实 Playwright 执行内核（Chromium/Firefox/WebKit）+ 失败截图与步骤日志
- [x] 真实录制（Playwright codegen）并解析为积木步骤，上传后自动出现在平台
- [x] 平台「真实节点与用例」页：节点、用例、执行记录与实时日志，可下发执行
- [ ] 安装包的公开下载托管（超出仓库单文件限制，需外部对象存储）
- [ ] Windows 代码签名与 macOS 公证（需开发者证书）

## 全量真实化（已完成）
- [x] 概览、用例、任务编排、进度看板、报告、节点、系统配置全部读写真实数据库
- [x] 任务状态与进度由真实执行记录收敛，报告与趋势由真实执行结果派生
- [x] 节点在线/离线按真实心跳判定，CPU/内存/并发能力由客户端真实上报
- [x] 升级推送与升级结果写入数据库，客户端轮询领取真实升级任务
- [x] 节点累计执行次数按真实执行结果累加
- [ ] 用例详情页：导出 Playwright 脚本按钮；用例按版本管理；平台下发任务自动携带脚本版本号

## 2026-09-09
- [x] 浏览器内核改为平台侧镜像下载，客户端只下载一次并本地缓存（安装包不含内核）
- [x] 任务编排支持用例依赖（串行）：前置通过才执行下一个，前置失败自动跳过
- [x] 客户端本地保留执行日志与结果，断网入 outbox，网络恢复后自动补传

## 待验证（本轮）
- [ ] 平台写真实用例并保存多版本，下发旧版本到客户端执行，核对流程图与日志一致
- [ ] 多台真实 Agent 并行接单：队列、进度、日志按设备区分，单台卡住不影响其他
- [ ] 单台 Agent 录制完整流程，用例详情页流程图/日志/版本/脚本完整回传

- [x] 发布清单改为数据库真实记录（agent_releases），CI 发布后自动登记
- [x] 升级轮询与升级回传接口加节点令牌校验，移除公开的升级推送/记录查询
- [x] 删除旧演示版 Electron 外壳，打包入口指向真实客户端 agent-app
- [ ] 需在 GitHub 与平台配置同一个 AGENT_RELEASE_TOKEN，以及 PLATFORM_URL

## 用例模板与设备注册（本轮）
- [x] 用例支持「模板」标记与参数默认值（用例详情页「参数化（模板变量）」面板）
- [x] 系统配置新增「参数绑定」：按环境 / 设备覆盖参数取值（优先级：设备 > 环境 > 用例默认值）
- [x] 下发任务时自动替换步骤、起始地址与脚本中的 ${参数名}，并回传缺失参数提醒
- [x] 用例列表对模板用例提供「用模板创建」一键派生
- [x] 下载页新增「注册设备并获取节点令牌」（需平台登录），支持重置令牌
- [x] 客户端支持用 PLAYFLOW_AGENT_TOKEN 预置令牌；注册/心跳、领任务、执行与升级回传全部校验令牌
- [ ] 登录后的端到端人工验收：当前平台还没有任何注册账号，需你先在登录页注册一个账号

## 条件循环与拖拽编辑（本轮）
- [x] 关键字积木新增条件 / 循环，客户端执行内核（agent-app/runner.cjs）真实解析并执行条件分支与循环
- [x] 循环变量 ${LOOP_INDEX} / ${当前循环} / ${循环次数}，与参数绑定共存，运行时由循环上下文替换
- [x] 用例编辑改为拖拽式积木编辑器（拖入插入、拖动排序、上下移动、删除），右侧脚本实时生成并置顶跟随
- [x] 端到端验收：SQLite 注册设备 AG-LOOP-01 → 上传含条件+3 次循环的用例 → 下发 → 真实 Chromium 执行通过（7.6s，29 条日志，循环截图 loop-1/2/3-of-3）写入本地数据库
- [x] 发布流程加入 macOS Developer ID 签名与 Apple 公证（缺证书时降级为未签名包）
- [ ] macOS 签名/公证需在 GitHub 配置证书密钥：MACOS_CERT_P12_BASE64、MACOS_CERT_PASSWORD、MACOS_SIGN_IDENTITY、APPLE_ID、APPLE_APP_PASSWORD、APPLE_TEAM_ID
