# PlayFlow

PlayFlow 管理自动化测试用例，并通过执行节点运行用例、收集结果。

## Language

**Agent 发布版本**：已经发布、供用户下载的 Agent 安装包版本。下载页展示此版本，它不代表所有设备都已安装该版本。
_Avoid_：在下载页中用含义不明确的“客户端当前版本”指代发布版本。

**Agent 运行版本**：某台执行节点当前实际运行的 Agent 版本。只有该设备运行了新版本，运行版本才随之改变。
_Avoid_：将下载完成或打开安装包称为已升级。

**Frame 上下文**：用例当前作用的文档范围，初始为主文档；进入 iframe 后，后续元素关键字只在该 Frame 中解析。
_Avoid_：用“切换页面”描述 iframe；页面导航与 Frame 上下文切换是不同操作。

**Frame 栈**：按进入顺序记录的 Frame 上下文。它支持嵌套 iframe、返回上层 Frame 和返回主文档。
_Avoid_：把 Frame 栈当成浏览器标签页或弹窗列表。

**待转换步骤**：录制器识别到、但当前关键字集合不能安全表达的 Playwright 操作。待转换步骤必须向用户展示原始语句，并阻止其作为完整可执行用例上传或运行。
_Avoid_：静默丢弃未支持的录制操作。
